@model Blog.DAL.Classes.Project
<div class="container">
    <div class="row">
        <div class="col-12">
            <h2 class="text-center">@Model.Subject</h2>
            <br />
            <p>@Html.Raw(@Model.Content)</p>
            <p class="text-right text-gray">@Model.CreatedDate.ToShortDateString()</p>
        </div>
    </div>
</div>
